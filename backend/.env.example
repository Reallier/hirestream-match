# TalentAI Backend 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ⚠️ 切勿将 .env 提交到版本控制！

# ============ DashScope / Qwen API ============
# 从阿里云 DashScope 控制台获取
DASHSCOPE_API_KEY=sk-your-api-key-here

# 模型配置
QWEN_MODEL=qwen3-max
OCR_MODEL=qwen-vl-ocr-2025-11-20

# ============ 用户认证配置 ============
# JWT 密钥（生产环境必须更换！至少 32 位随机字符串）
# 可使用: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_IN_PRODUCTION_USE_RANDOM_32_BYTES
JWT_ALGORITHM=HS256

# 认证模式
# mock - 开发测试（自动登录为测试用户）
# jwt - 生产环境（从 Cookie 验证 JWT）
USER_AUTH_MODE=jwt

# 新用户默认免费额度（元）
DEFAULT_FREE_QUOTA=1.0

# ============ 数据库配置 ============
# 开发环境可用 SQLite
DATABASE_URL=sqlite:///./data/hirestream.db

# 生产环境使用 PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/talentai
# AUTH_DATABASE_URL=postgresql://user:password@host:5432/intjtech_users

# 数据库密码（PostgreSQL）
DATABASE_PASSWORD=

# ============ 管理员 API ============
# 用于 /api/reindex 等管理接口
# 可使用: openssl rand -hex 16
ADMIN_API_KEY=

# ============ 系统提示词 ============
# 可选，留空使用默认提示词
SYSTEM_PROMPT=
USER_TEMPLATE=
