# ============================================
# TalentAI 企业私有化部署 - 默认配置
# ============================================
#
# 🚀 一键启动: docker compose -f compose.private.yml up -d
#
# 📝 必须修改的配置只有一项: DASHSCOPE_API_KEY
#    其他配置都有合理的默认值，可直接使用
#
# ============================================

# ===== 必须配置 =====

# 阿里云 DashScope API Key（获取首个 AI 功能必须配置）
# 申请地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-你的API密钥

# ===== 可选配置（有默认值，可不修改）=====

# 镜像版本
VERSION=private-latest

# 端口配置
FRONTEND_PORT=3000
BACKEND_PORT=8000

# 数据库密码（默认已设置强密码）
# DB_PASSWORD=TalentAI2026!

# JWT 密钥（默认已设置，生产环境建议修改）
# JWT_SECRET=talentai-private-jwt-secret-2026

# Qwen 模型（默认 qwen-max）
# QWEN_MODEL=qwen-max

# ===== License 配置（激活后配置）=====

# 认证模式: saas / private / offline
AUTH_MODE=private

# License 校验（首次部署设为 false，激活后设为 true）
LICENSE_CHECK_ENABLED=false

# License 公钥（激活时由 INTJsys 提供）
# LICENSE_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
