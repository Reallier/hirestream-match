# ============================================
# HireStream Match 环境变量配置示例
# ============================================

# 数据库连接（与 TalentAI 共享 PostgreSQL）
# 本地开发：postgresql://talentai:YOUR_PASSWORD@localhost:5432/talentai
# Docker 部署：postgresql://talentai:YOUR_PASSWORD@talentai-db:5432/talentai
DATABASE_URL=postgresql://talentai:YOUR_PASSWORD@talentai-db:5432/talentai
DATABASE_PASSWORD=YOUR_PASSWORD  # 必需：数据库密码

# 阿里云 DashScope API Key（用于 Qwen 模型调用）
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Qwen 模型配置
QWEN_MODEL=qwen-max-2025-01-25

# JWT 配置（必须与官网 INTJTech_04 保持一致！）
# ⚠️ 必需：不再有默认值，必须显式配置
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256

# 用户认证模式：mock（开发测试）或 jwt（生产环境）
USER_AUTH_MODE=jwt

# 用户提示词模板（用于简历匹配分析）
USER_TEMPLATE="请分析以下职位描述和简历的匹配度...(完整模板)"

# CORS 配置（多个域名用逗号分隔）
# 默认白名单：talentai.reallier.top, intjtech.cn, localhost
# 可通过此变量覆盖默认值
CORS_ORIGINS=https://talentai.reallier.top:5443,https://intjtech.cn

# 管理后台凭证（强烈建议修改默认值！）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
ADMIN_JWT_SECRET=your-admin-jwt-secret
